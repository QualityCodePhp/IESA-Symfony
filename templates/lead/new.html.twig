<!DOCTYPE html>

<title>New Lead</title>

{% block body %}
    <h1>Create new Lead</h1>

    {{ include('lead/_form.html.twig') }}

    <a href="{{ path('lead_index') }}">back to list</a>
{% endblock %}