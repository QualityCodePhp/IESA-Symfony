<!DOCTYPE html>

<title>Edit Lead</title>

{% block body %}
    <h1>Edit Lead</h1>

    {{ include('lead/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('lead_index') }}">back to list</a>

    {{ include('lead/_delete_form.html.twig') }}
{% endblock %}